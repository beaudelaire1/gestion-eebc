name: Runner Smoke

on:
  push:
    branches: [develop]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Confirm GitHub-hosted runner starts
        run: |
          echo "runner-started"
          uname -a
